name: ensemble_protos
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

publish_to: none

environment:
  sdk: ^3.3.0-135.0.dev

# Add regular dependencies here.
dependencies:
  grpc: ^3.2.4
  protobuf: ^3.1.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
